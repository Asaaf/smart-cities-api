{
  "routes": [
    {
      "method": "GET",
      "path": "/countries",
      "handler": "countries.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/countries/:id/provinces",
      "handler": "countries.provinces",
      "config": {
        "policies": []
      }
    }
  ]
}

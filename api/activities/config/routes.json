{
  "routes": [
    {
      "method": "GET",
      "path": "/activities",
      "handler": "activities.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/activites/tourists",
      "handler": "activities.tourists",
      "config": {
        "policies": []
      }
    }
  ]
}

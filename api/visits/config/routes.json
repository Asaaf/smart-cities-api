{
  "routes": [
    {
      "method": "GET",
      "path": "/visits",
      "handler": "visits.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/visits/cities",
      "handler": "visits.cities",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/visits",
      "handler": "visits.create",
      "config": {
        "policies": []
      }
    }
  ]
}

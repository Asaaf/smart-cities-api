{
  "routes": [
    {
      "method": "GET",
      "path": "/visits",
      "handler": "visits.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/visits/:id",
      "handler": "visits.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/visits",
      "handler": "visits.create",
      "config": {
        "policies": []
      }
    }
  ]
}

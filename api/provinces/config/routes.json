{
  "routes": [
    {
      "method": "GET",
      "path": "/provinces",
      "handler": "provinces.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/provinces/:id/cities",
      "handler": "provinces.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}

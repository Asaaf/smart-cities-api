{
  "routes": [
    {
      "method": "GET",
      "path": "/tourists",
      "handler": "tourists.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/tourists",
      "handler": "tourists.create",
      "config": {
        "policies": []
      }
    }
  ]
}

{
  "routes": [
    {
      "method": "GET",
      "path": "/tourists/:email",
      "handler": "tourists.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/tourists",
      "handler": "tourists.create",
      "config": {
        "policies": []
      }
    }
  ]
}
